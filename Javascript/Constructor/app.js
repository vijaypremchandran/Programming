/**
 * Constructor - used to create a modern javascript object.
 *   - This has object defined. 
 *   - constructor defined with the properties of the object.
 *   - function/methods defined.
 *   - A class can be instatiated with New keyword in other applications. 
 *     passing new sets of values.
 */

// This app uses the calculator app.

let calc = new Calculator(1,2);

console.log(calc.Add());
console.log(calc.Subtract());
console.log(calc.Multiply());
console.log(calc.Divide());